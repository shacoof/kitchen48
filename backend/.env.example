# Database Configuration
# Copy this file to .env and update values as needed

# Local development (Docker)
DATABASE_URL="postgresql://kitchen48_user:kitchen48_dev_password@localhost:5433/kitchen48_dev"

# For DBaaS with connection pooling (e.g., Neon, Supabase)
# Use DIRECT_DATABASE_URL for migrations, DATABASE_URL for pooled connections
# For local dev, both point to the same database
DIRECT_DATABASE_URL="postgresql://kitchen48_user:kitchen48_dev_password@localhost:5433/kitchen48_dev"

# Server Configuration
PORT=3000
NODE_ENV=development
